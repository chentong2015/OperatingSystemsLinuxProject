> ps -A | grep java 查看指定程序的进程Process
> ps -e | grep ssh 检查当前的ssh开启情况
> kill -9 [PID]     关闭指定进程

> time command      确定某个指令执行的时间
> nohup command     即使关闭Terminal仍然运行指令

> top              查看系统平均负载和CPU使用率fr
> free –m          查看内存的使用情况
> df -h            查看剩余存储空间(disk free)
  -h, --human-readable
> du -sh /folder   查看指定目录大小(disk usage)
  -s, --summarize
  -h, --human-readable

如果有sshd，则ssh-server已经启动；若仅有agent，则尚未启动；
开启ssh服务：/etc/init.d/ssh start
# >cd /etc/init.d
# >./ssh start
# > netstat -tl
> netstat -anp | grep :22
> service sshd start 启动

> ifconfig IP地址确定 + 登陆的用户名和密码

关闭服务器的防火墙Fireware，内网使用来进行测试
> sudo ufw disable
> sudo ufw enable
> service iptables stop
> service iptables status # Stopped IPv4 firewall with iptables (tester stop)

> sudo apt-get install openssh-server 安装打开ssh服务的库